var menuState = {

	create: function(){

		// Our tiled scrolling background
	    space = game.add.tileSprite(0, 0, 4000, 4000, 'space');
			
		// var titleLabel = game.add.text(0,0,'Epic Space Battle',{font:'30px Arial',fill:'#ffffff', align: "center", boundsAlignH: "center", boundsAlignV: "middle"});
		// titleLabel.setTextBounds(0, 0, window.innerWidth * window.devicePixelRatio, (window.innerHeight * window.devicePixelRatio) - 100);
		// var startLabel = game.add.text(0,0,'Click to Start',{font:'20px Arial',fill:'#ffffff', align: "center", boundsAlignH: "center", boundsAlignV: "middle"});
		// startLabel.setTextBounds(0, 0, window.innerWidth * window.devicePixelRatio, window.innerHeight * window.devicePixelRatio);

	},

	update: function(){
		// if(game.input.activePointer.justPressed()) {
	 	// 	game.state.start('lobby');
	 	// }
	}

};